2019-09-02:
    - Detach configuration, no need to modify the script itself anymore
    - Optionally scan within ZIP files too
    - Use DETECT by default
    - Use $HOME instead of a full path
    - Do not delete all playlists, only generated ones
    - Other bugfixes